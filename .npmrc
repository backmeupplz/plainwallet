# Supply chain: package.json pins exact versions and package-lock.json pins every transitive one by integrity hash
# (install with `npm ci`, which refuses to change the lockfile). No dependency needs an install script, so none runs;
# `wxt build` and `wxt` (dev) generate the .wxt types themselves.
save-exact=true
ignore-scripts=true
